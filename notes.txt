complete export and share functionality or after filter results (with colors and same nice UI as timeline)
(done)multiselect tags in home page and view all those in those respective color blocks.
(done)modern attractive details page layout
(done)if any date and time unknown, give option to select that the date or time is unknow and save and dispaly the same.
mylife-events (primary),relatives-events,friends-events, other-events (or something similar) - include them when adding event and show the tag in timeline and filters as well.
(done)if any event has multiple occurances like dental appointment can have 10 sittings each on different date and all should be able to add properly with an option when adding and show in timeline and details page as well cleanly.
option to select which color to which tag.
add option for reminders.

2.are we storing all the info under user specific? both in local and in frebase
4. save and fetch all the info locally as well before storing or getting to / from firebase. so that the impact is less for DB.
informative and attractive home screen with may be one card as this event last n years ago or smt.

npx expo prebuild (to generate the native android and ios code. not eject, still can use expo related stuff but can be used to build the apps locally.)
npx expo run:android --variant debug (connect the mobile to laptop and run this cmd to build the app, generate the apk and install to the mobile with hot reload, connceted to dev server.)
npx expo run:android --variant release (same as above but does not connect to metro and hence no need to run the server and no hot reloading.)
expo start -d (if we just want to run the app on mobile. if not installed any new packages then this cmd is enough no need for above cmd.) 
npx expo start -c